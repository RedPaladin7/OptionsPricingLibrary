[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "options_lib"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["options_lib*"]